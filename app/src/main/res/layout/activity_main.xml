<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layoutDescription="@xml/activity_main_scene"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/choosePictureButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_marginTop="14dp"
        android:layout_marginEnd="14dp"
        android:layout_marginRight="14dp"
        android:contentDescription="@string/choosePictureButtonDesc"
        android:text="@string/choosePictureButtonText" />

    <ImageView
        android:id="@+id/ticketImageView"
        android:layout_width="match_parent"
        android:layout_height="814dp"
        android:layout_alignParentTop="true"
        android:layout_marginTop="76dp"
        android:contentDescription="@string/ticketImageViewDesc" />

</RelativeLayout>